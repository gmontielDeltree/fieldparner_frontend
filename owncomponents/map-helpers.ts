import { Map } from "mapbox-gl";

const mapa_seleccionar_campo = (map: Map, nombre) => {};

const mapa_seleccionar_lote = (map: Map, nombre) => {};

const mapa_seleccionar_nada = (map: Map) => {};

const mapa_mostrar_campos = (map: Map, nombre) => {};

const mapa_imagen_satelital = (map: Map, nombre) => {};

const mapa_mapa_ambientes = (map: Map, nombre) => {};

export {
  mapa_seleccionar_campo,
  mapa_seleccionar_lote,
  mapa_seleccionar_nada,
  mapa_mostrar_campos,
  mapa_imagen_satelital,
  mapa_mapa_ambientes,
};
